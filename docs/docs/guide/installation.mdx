---
title: Installation
description: How to install ReactPress in your project
---

# Installation

ReactPress is available as an npm package and can be installed in any React project.

## Prerequisites

Before you begin, make sure you have the following installed:

- **Node.js** version 18 or higher
- **Bun** or **npm/pnpm/yarn** package manager

## Quick Install

:::code-group

```bash [npm]
npm install @sylphx/reactpress @sylphx/reactpress-theme-default
```

```bash [bun]
bun add @sylphx/reactpress @sylphx/reactpress-theme-default
```

```bash [pnpm]
pnpm add @sylphx/reactpress @sylphx/reactpress-theme-default
```

```bash [yarn]
yarn add @sylphx/reactpress @sylphx/reactpress-theme-default
```

:::

## Create a New Project

If you're starting from scratch, you can create a new ReactPress project using our starter template:

:::code-group

```bash [npm]
npm create reactpress@latest my-docs
cd my-docs
npm install
npm run dev
```

```bash [bun]
bun create reactpress my-docs
cd my-docs
bun install
bun run dev
```

:::

## Manual Setup

If you prefer to set up ReactPress manually:

### 1. Create Project Structure

```
my-docs/
├── docs/              # Your markdown files
│   ├── index.mdx
│   ├── guide/
│   └── api/
├── src/
│   └── main.tsx       # React entry point
├── index.html         # HTML template
├── reactpress.config.ts
├── vite.config.ts
└── package.json
```

### 2. Configure ReactPress

Create `reactpress.config.ts`:

```typescript
import { defineConfig } from "@sylphx/reactpress";

export default defineConfig({
  title: "My Documentation",
  description: "My awesome documentation site",
  theme: {
    nav: [
      { text: "Guide", link: "/guide" },
      { text: "API", link: "/api" }
    ],
    sidebar: [
      {
        text: "Getting Started",
        items: [
          { text: "Introduction", link: "/" },
          { text: "Installation", link: "/installation" }
        ]
      }
    ]
  }
});
```

### 3. Create Entry Point

Create `src/main.tsx`:

```typescript
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import { BrowserRouter, Route, Routes } from "react-router-dom";
import { Layout } from "@sylphx/reactpress-theme-default";
import "@sylphx/reactpress-theme-default/style.css";

// @ts-expect-error - Generated by Vite plugin
import { routes } from "virtual:reactpress/routes";
import config from "./config.json";

createRoot(document.getElementById("root")!).render(
  <StrictMode>
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<Layout config={config} />}>
          {routes.map((route: any) => (
            <Route key={route.path} path={route.path} element={<route.component />} />
          ))}
        </Route>
      </Routes>
    </BrowserRouter>
  </StrictMode>
);
```

### 4. Configure Vite

Create `vite.config.ts`:

```typescript
import { defineConfig } from "vite";
import { createReactPressPlugin, routesPlugin, loadConfig } from "@sylphx/reactpress";
import { resolve } from "node:path";

const docsDir = resolve(process.cwd(), "docs");
const config = await loadConfig(process.cwd());

export default defineConfig({
  plugins: [routesPlugin(docsDir), ...createReactPressPlugin(config)]
});
```

### 5. Add Scripts

Add to `package.json`:

```json
{
  "scripts": {
    "dev": "vite",
    "build": "bun run build.ts",
    "preview": "vite preview"
  }
}
```

### 6. Create Your First Page

Create `docs/index.mdx`:

```markdown
---
title: Welcome
---

# Welcome to My Docs

This is my documentation site built with ReactPress!
```

## Verify Installation

Start the development server:

:::code-group

```bash [npm]
npm run dev
```

```bash [bun]
bun run dev
```

:::

Open your browser to `http://localhost:5173` to see your site!

## What's Next?

- [Configure your site](/guide/configuration)
- [Learn about Markdown features](/guide/markdown)
- [Customize the theme](/guide/theming)
