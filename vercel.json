{
	"buildCommand": "bun install && bun run build && bun --cwd docs run build",
	"outputDirectory": "docs/dist",
	"installCommand": "bun install",
	"framework": null,
	"rewrites": [
		{
			"source": "/(.*)",
			"destination": "/index.html"
		}
	]
}
